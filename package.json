{
  "name": "lamb",
  "version": "1.0.0",
  "description": "A terminal explorer for the OCaml native compiler pipeline and tagged value representation.",
  "type": "module",
  "private": true,
  "bin": {
    "lamb": "./lamb"
  },
  "scripts": {
    "test": "node --test test.js",
    "check": "node --check core.js && node --check cli.js && node --test test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
