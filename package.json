{
  "name": "lamb",
  "version": "1.0.0",
  "description": "A terminal explorer for the OCaml native compiler pipeline and tagged value representation.",
  "type": "module",
  "private": true,
  "bin": {
    "lamb": "./bin/lamb"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "node --check src/compiler.js && node --check src/cli/index.js && node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
